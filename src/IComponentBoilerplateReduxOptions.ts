namespace IIIFComponents{
    export interface IComponentBoilerplateReduxOptions extends _Components.IBaseComponentOptions {
        color?: string; // set the boundary color
        size?: number; // set the initial square size
    }
}
